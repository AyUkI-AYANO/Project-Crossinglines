# Minecraft 1.21.4 Fabric 铁路系统可视化 Mod 设计方案

## 1. 目标与范围

本 Mod 面向 **Minecraft Java 1.21.4 + Fabric**，提供一个可视化界面来：

1. 管理线路（创建、编辑、删除、重建）。
2. 自动生成两点间铁路（地面线/地下线）。
3. 根据地形进行最优路径规划（尽量降低土方与施工复杂度）。
4. 根据铁路类型自动配置配套设施（路灯、地基、围挡、检修通道等）。

---

## 2. 技术栈建议

- **Loader / API**: Fabric Loader + Fabric API（与 1.21.4 对应版本）
- **构建**: Gradle + Loom
- **GUI**:
  - 原生屏幕：`Screen` + `DrawContext`
  - 可选增强：Cloth Config（用于全局配置页）
- **路径规划**:
  - A*（基础）
  - 可拓展为 Theta* / JPS（性能优化）
- **数据持久化**:
  - 世界维度级存档：`PersistentState`
  - 配置文件：JSON（全局默认参数）

---

## 3. 功能拆分（MVP -> 增强）

## 3.1 MVP（建议第一阶段）

1. 可视化界面选择起点、终点、铁路类型。
2. 执行一次路径搜索并预览路线（粒子线或临时方块标记）。
3. 玩家确认后生成轨道与基础。
4. 保存线路记录，可在“线路管理”界面查看并删除。

## 3.2 增强（第二阶段）

1. 地下铁路自动处理：
   - 隧道开挖
   - 支撑结构
   - 照明
   - 通风井（可选风格）
2. 路线分段施工（防止卡顿）：每 tick 放置固定数量方块。
3. 多目标优化：成本、平滑度、坡度限制、桥隧比例。
4. 样式模板系统（现代/工业/木质风格）。

---

## 4. 系统架构

```text
Client UI
  ├─ RoutePlannerScreen (选点/参数/预览)
  ├─ LineManagerScreen  (线路列表管理)
  └─ PreviewRenderer    (渲染路径)

Shared Logic
  ├─ RailGraphBuilder   (候选节点生成)
  ├─ PathFinder         (A* 搜索)
  ├─ CostModel          (地形代价函数)
  ├─ SlopeConstraint    (坡度约束)
  ├─ RailBlueprint      (轨道+配套蓝图)
  └─ FacilityPlanner    (地基/路灯等)

Server Execution
  ├─ BuildTaskQueue     (分段施工队列)
  ├─ BlockPlacer        (安全放置)
  └─ RailLineState      (PersistentState)
```

---

## 5. 路径规划设计（核心）

## 5.1 节点定义

每个节点可定义为：

- `BlockPos pos`
- `int yMode`（地面模式 / 地下模式）
- `int direction`（用于惩罚急弯）

为了控制搜索规模，推荐：

- 地面模式仅在地表及其附近高度采样。
- 地下模式固定设计埋深区间（如地表下 6~12 格）。

## 5.2 成本函数（建议）

`f(n) = g(n) + h(n)`，其中 `g(n)` 可拆分：

- 基础移动成本：1
- 高程变化成本：`dy * w_height`
- 转弯惩罚：`turnPenalty * w_turn`
- 隧道开挖成本：`breakBlocks * w_tunnel`
- 架桥成本：`bridgeLength * w_bridge`
- 生态/危险区域惩罚（岩浆、水体、村庄保护区）：`hazard * w_hazard`

`h(n)` 使用到终点的曼哈顿或欧几里得距离。

> 这样可以实现“最短 ≠ 最优”，而是偏向施工成本更低、更平滑、维护性更高的路线。

## 5.3 坡度约束

MC 铁路对坡度敏感，建议限制：

- 连续上坡长度阈值（避免长坡影响矿车速度）
- 单段最大坡度（例如每 8 格水平最多升 1 格，可配置）

在扩展节点时直接过滤违规候选。

---

## 6. 配套设施自动化规则

## 6.1 普通地面铁路

- 轨道两侧 `offset=2~3` 放置路灯柱（间距可配置，如 8~12 格）。
- 地基优先使用耐久方块（石砖/混凝土）。
- 遇水域自动改桥面结构。

## 6.2 地下铁路

- 隧道截面固定模板（例如 5x5 或 7x5）。
- 每隔 N 格布置照明。
- 隧道底层铺“道床方块 + 铁轨”。
- 可选逃生通道/检修道（提高可玩性）。

## 6.3 设施策略接口（建议）

```java
public interface FacilityPolicy {
    void decorateSegment(World world, RailSegment segment, BuildContext context);
}
```

不同线路类型挂载不同 `FacilityPolicy`，便于扩展。

---

## 7. GUI 交互流程

1. 打开“铁路规划器”界面。
2. 点击“选择起点”（在世界中看向方块并确认）。
3. 点击“选择终点”。
4. 选择铁路类型：
   - 普通铁路
   - 地下铁路
5. 设置参数：
   - 预算优先 / 速度优先
   - 坡度阈值
   - 路灯间距
6. 点击“预览路线”。
7. 预览满意后点击“开始施工”。
8. 在“线路管理”中查看状态（施工中/已完成/失败）。

---

## 8. 性能与稳定性建议

1. **分段施工**：每 tick 限制方块操作数，避免服务器卡顿。
2. **可中断任务**：玩家可暂停/取消施工。
3. **区块加载检查**：只在已加载区块施工，防止越界问题。
4. **异常回滚（可选）**：记录修改清单，支持部分撤销。

---

## 9. 数据结构建议

```java
record RailLine(
    UUID id,
    String name,
    BlockPos start,
    BlockPos end,
    RailType type,
    List<BlockPos> path,
    RailSettings settings,
    BuildStatus status
) {}
```

`RailLineState` 负责：

- 新建线路 ID 分配
- 状态更新（排队/施工/完成/失败）
- 世界保存与读取

---

## 10. Fabric 端实现步骤（落地）

1. 初始化工程（Loom + Fabric API + 基础 Mod 入口）。
2. 定义网络包：
   - 客户端请求规划
   - 服务端返回预览路径
   - 客户端请求开始施工
3. 实现 A* 和成本模型。
4. 实现预览渲染。
5. 实现施工队列与方块放置。
6. 实现 PersistentState 存档。
7. 做参数配置页与线路管理页。

---

## 11. 开发风险与应对

1. **路径搜索过慢**
   - 解决：限制搜索边界、分层搜索、缓存地形采样。
2. **地形破坏过大**
   - 解决：提高开挖成本权重，默认绕行。
3. **多人服同步问题**
   - 解决：所有最终建造逻辑放服务端执行。
4. **版本兼容问题**
   - 解决：锁定 1.21.4，抽象 NMS 敏感层。

---

## 12. 下一步（建议）

如果你愿意，我可以在下一步直接给你一个可运行的 **Fabric 1.21.4 初始代码骨架**，包含：

1. 基础 Mod 入口与命令。
2. 规划 GUI（起终点与类型选择）。
3. A* 简化实现。
4. “普通铁路/地下铁路”两套设施策略接口与默认实现。
5. 可直接编译运行的最小 Demo。

